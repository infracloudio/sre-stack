{{- if ne .Values.stack_mode "eks" }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: mysql-config
# Also set in the Secret
data:
  my-cnf: |
    [client]
    user=root
    password={{ .Values.mysql_root_password }}
{{- end }}