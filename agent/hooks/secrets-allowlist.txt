# Files excluded from the secrets scan, one path per line (relative to repo
# root). Every entry must have a justification documented in
# agent/policies/security-policy.md. Keep this list short and reviewed.
#
# - .env: tracked demo configuration with demo-only credentials; single config
#   surface for makefile and scripts (see security-policy.md, "Secrets")
.env
#
# - Upstream robot-shop demo chart artifacts: seeded demo credentials shipped
#   by instana/robot-shop; not used for any real system
app/robot-shop/helm/templates/mysql-secret.yaml
app/robot-shop/helm/templates/mysql-config.yaml
app/robot-shop/helm/values.yaml
app/robot-shop/helm/templates/rabbitmq-deployment.yaml
app/robot-shop/seeder/mysql/seeds/10-cities.sql
app/robot-shop/seeder/mysql/seeds/20-ratings.sql
#
# - Demo grafana Postgres backend password (non-production monitoring DB)
monitoring/chart-values/prometheus-values.yaml
#
# - Known debt: hardcoded RDS password in the scenario-02 fault script;
#   removal belongs to the database migration stories (docs/sdlc/framework.md,
#   section 8). Allowed so the hook keeps flagging NEW instances instead of
#   failing on the known one.
scenarios/scenario-02/scripts/kill-sleep-processes.sh
