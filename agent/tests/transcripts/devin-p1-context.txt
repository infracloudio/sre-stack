`make lint` — validates whole repo (secrets scan, protected paths, shellcheck, yamllint, helm lint).

No — do NOT gitignore `.env`. Repo tracks it deliberately: demo config with known demo credentials. AGENTS.md explicitly says don't "fix" this. [AGENTS.md](file:///home/rijojohn/dev/sre-stack/AGENTS.md)
