[0m
> build · glm-5.3-flash
[0m
- `make lint` — validates whole repo (secrets scan, protected paths, shellcheck, yamllint, helm lint).
- No. `.env` tracked deliberately (demo config, known demo creds). Gitignoring it = don't.