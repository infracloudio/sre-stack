# shellcheck shell=bash disable=SC2034
# spot-fits: 30 spot vCPU available >= 26 needed; every workload pool add
# carries the spot flags, the system pool's create carries none.
SIGNED_IN=1
SPOT_LIMIT=30
POOL_MODE=spot
