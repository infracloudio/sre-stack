# shellcheck shell=bash disable=SC2034
# usage-malformed (T058): the allowance answer is not a usage array. The
# helper must refuse plainly with no creates — never crash or assume room.
SIGNED_IN=1
USAGE_MALFORMED=1
